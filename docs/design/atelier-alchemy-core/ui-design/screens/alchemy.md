# 調合画面 詳細設計 ★ゲームの核心

作成日: 2026-08-04
準拠要件: [`../../../../spec/atelier-alchemy-core/requirements.md`](../../../../spec/atelier-alchemy-core/requirements.md) §3「調合（主戦場）」

## 基本情報

| 項目 | 値 |
|------|-----|
| 画面ID | SCR-002 |
| 親画面 | なし（MainScene内の常駐タブの1つ） |
| 子画面 | ギルド納品画面（調合実行後に自動遷移/オーバーレイ表示） |

## ワイヤーフレーム

```
┌─────────────────────────────────────────────┐
│ [庭][調合]タブ ランクノルマバー 残りターン数 所持G │ ← RankHud（常時表示、全画面共通。[overview.md](../overview.md)参照）
├─────────────────────────────────────────────┤
│ 本日の指定調合物: {品目/特性傾向}              │
├─────────────────────────────────────────────┤
│ レシピ: [▼ {選択中のレシピ名}]（解禁済みから選択）│
├─────────────────────────────────────────────┤
│  投入枠1  投入枠2  投入枠3  投入枠4  (枠5※強化時)│
│  [空]     [薬草B]  [空]     [空]               │
├─────────────────────────────────────────────┤
│ プレビュー: 品質{score} 発現特性[聖][金]        │
│            見込み貢献度{x} / 見込み報酬{y}      │
├─────────────────────────────────────────────┤
│  在庫一覧（投入元）                            │
│  [薬草A][薬草B][鉱石S(聖)][鉱石A(聖)]...       │
├─────────────────────────────────────────────┤
│   [調合を実行する]  [ショップ]  [ターンを終了する] │
└─────────────────────────────────────────────┘
```

🟡 レイアウトは要件定義書§3・§4の記述（投入枠4枠、在庫から選ぶ、入れ替え/取り消し可）から本文書で組み立てた提案。

## UI要素

| 要素ID | 種類 | 説明 | 状態 |
|--------|------|------|------|
| dropdown-recipe | レシピ選択ドロップダウン | 解禁済みレシピから1つを事前に選択する（🔵2026-08-04ヒアリングで事前選択方式に確定、[`core-systems.md`](../core-systems.md) AlchemySystem節参照）。未選択の場合`btn-execute`は無効化される | 未選択/選択済み |
| slot-{n} | 投入枠 | 在庫から選んだ素材を配置する。タップ/ドラッグで投入、再タップで取り消し | 空/投入済み |
| card-inventory-{id} | 在庫カード | 品質・特性タグをアイコン表示。投入済みのものは選択不可表示にする | 選択可能/投入済み（選択不可） |
| txt-preview-quality | テキスト | 現在の投入内容から算出される品質スコアのライブプレビュー | - |
| txt-preview-traits | テキスト | 現在の投入内容から発現見込みの特性一覧（2個未満のタグは「あと1個」等のヒント表示、🟡TBD） | - |
| txt-preview-value | テキスト | 見込み貢献度・見込み報酬のライブプレビュー | - |
| btn-execute | プライマリボタン | 調合を実行する。レシピ未選択、投入枠が0個、または投入枠上限を超える場合は無効化（🔵要件定義書§3「調合（主戦場）」「0個投入では実行不可」＋レシピ事前選択の必須化＋上限チェック） | 有効（レシピ選択済み・1個以上`max_slots`以下投入）/無効（未選択・0個・上限超過） |
| btn-clear-slot-{n} | セカンダリボタン | 個別の投入枠を取り消す | 投入済みの枠にのみ表示 |
| btn-shop | セカンダリボタン | 工房強化・ショップ画面へオーバーレイ表示（[garden.md](./garden.md)の`btn-shop`と同一機能）（🔴2026-08-06追加、実装レディネス監査対応。旧版は`btn-shop`がgarden.mdにしか存在せず、`ui-design/overview.md`の画面遷移図が定義する「調合画面からもショップへ行ける」経路の実体がなかった） | 常時有効 |
| btn-end-turn | プライマリボタン | ターンを終了する（[garden.md](./garden.md)の`btn-end-turn`と同一機能）（🔴2026-08-06追加） | 常時有効 |

## 状態遷移

### 初期状態（在庫あり・投入枠すべて空）

「調合を実行する」ボタンは無効表示。プレビューは空。

### 投入中状態

素材を1つ投入するたびに、`txt-preview-quality`・`txt-preview-traits`・`txt-preview-value`をリアルタイム再計算して更新する（[`core-systems.md`](../core-systems.md) `QualityCalculator`/`TraitActivation`/`ProductValueCalculator`**に加えて`DeliveryResolver`（指定合致ボーナス判定）まで**をUIから同期呼び出しする。🔵2026-08-05修正、PRレビューCritical#4対応。旧版は`ProductValueCalculator`までしか呼んでおらず、指定合致ボーナス抜きの値が表示され実際の納品結果と食い違っていた）。**この画面の核心体験**は、投入を1つ変えるたびに「品質を盛るか特性を宿すか」のトレードオフがプレビューに反映され、プレイヤーが一瞬迷うことである（コンセプト文書§6「検証すべき最優先の仮説」参照🔵）。

### 実行可能状態

レシピが選択済みで、かつ投入枠が1個以上埋まると`btn-execute`が有効化される。

### エラー状態

- 在庫が空の場合: 在庫一覧に「収穫した素材がありません」等の空状態表示（🟡TBD文言）

### 実行後状態

調合実行後、投入素材は在庫から消費され、投入枠は空にリセットされる。完成品はギルド納品画面へ引き継がれる（[`dataflow.md`](../dataflow.md) シーケンス図参照）。

## アニメーション

| トリガー | アニメーション | 時間 | イージング |
|----------|---------------|------|-----------|
| 素材投入時 | カードが在庫から投入枠へスライド | 🟡TBD | 🟡TBD |
| 特性発現（2個目投入で条件達成） | 該当スロットが光る等のフィードバック | 🟡TBD | 🟡TBD |
| 調合実行時 | 完成品の生成演出→ギルド納品画面への遷移演出 | 🟡TBD | 🟡TBD |

## イベント

| イベント名 | トリガー | 処理内容 |
|-----------|----------|----------|
| OnMaterialSlotted | 在庫カードをタップ/ドラッグ | 投入枠に配置し、プレビューを再計算 |
| OnSlotCleared | 投入枠のクリアボタン押下 | 投入を取り消し、在庫に戻す |
| OnAlchemyExecuted | 「調合を実行する」押下 | `GameState.execute_alchemy(selected_recipe_id, slot_materials)`呼び出し（🔵2026-08-05修正、Warning対応。旧版は引数が`slot_materials`のみでdataflow.mdと不一致だった。[`dataflow.md`](../dataflow.md) シーケンス図参照） |
| OnShopOpened | 「ショップ」押下 | 工房強化・ショップ画面をオーバーレイ表示（🔴2026-08-06追加） |
| OnTurnEnded | 「ターンを終了する」押下 | `GameState.end_turn()`を呼び出す（🔴2026-08-06追加。[garden.md](./garden.md)と同一処理） |

## アクセシビリティ

- [ ] キーボード操作対応（🟡TBD。ドラッグ操作の代替として矢印キー+決定キーでの投入枠選択を想定するが未確定）
- [ ] スクリーンリーダー対応（🔴未定）
- [x] 色覚多様性対応（特性タグは色だけでなくアイコン形状でも区別する、`.claude/rules/design-guide.md`踏襲🔵）
- [ ] フォーカス表示（🟡TBD）
