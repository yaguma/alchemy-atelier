# Verification Report - 2026-08-20（タスク015〜018）
## Plan: garden

## Summary

| Item | Result |
|------|--------|
| Tasks | 4/4 completed（015〜018） |
| Tests | 516 passed, 0 failed, 0 skipped（0 errors / 0 flaky / 0 orphans、55 suites） |
| Coverage | N/A（GDScript/gdUnit4に標準カバレッジ計測機構なし。数え上げ基準で別途確認、下記参照） |
| Build | N/A（エクスポートプリセット未確定） |
| Lint | OK（`gdlint atelier/features/ atelier/shared/ atelier/autoload/`: Success: no problems found） |
| Format | OK（本plan対象ファイルは全て`gdformat --check`パス。`test_recipe_fixture_load.gd`未整形は本plan外・既存の既知事項） |
| 500-line rule | 1 file over limit（`atelier/autoload/game_state.gd` 895行。既知の受容事項、詳細下記） |

## Task Status

| ID | Title | Status |
|----|-------|--------|
| 015 | GameState.advance_turn_growth()を実装する | done |
| 016 | PlantSlotView（庭スロット表示コンポーネント）を実装する | done |
| 017 | SeedInventoryList（手持ち種一覧コンポーネント）を実装する | done |
| 018 | GardenScreen（庭画面本体）を実装する | done |

全4タスクが`status: done`であることをfrontmatterで確認した。garden plan全18タスクが完了。

## 実行方式に関する注記

015〜017は当初3体の並行バックグラウンドサブエージェント（`isolation`なし、`general-purpose`）へ委譲したが、3体ともAPIセッション上限（`resets 5am Asia/Tokyo`）に達して`idle_notification(failed)`で停止した。中断時点の成果物を確認したところ、017（SeedInventoryList）は実装・テストとも完成済み、015（advance_turn_growth）は実装・テストとも完成済み（未検証のまま停止）、016（PlantSlotView）はテストファイルのみで実装ファイル（`plant_slot_view.gd`/`.tscn`）が存在しない状態だった。セッション再開後、メインエージェントが直接（サブエージェント委譲なしで）以下を実施した:

1. 015・017は既存実装に対しテスト実行のみ行い、Green確認（追加実装は不要だった）
2. 016はテストファイル（`test_plant_slot_view.gd`、12テストケース）から要求インターフェースを読み取り、`plant_slot_view.gd`/`.tscn`を新規実装
3. 018（GardenScreen）はメインエージェントが直接、Interfacesに基づきテスト→実装の順で新規実装
4. 018実装時、タスクファイルのInterfacesに無い設計ギャップ（`SeedMaster`辞書・庭スロット総数がいずれも`GameState.get_state()`に含まれていない）が判明したため、`GameState.get_state()`に`seed_masters`・`garden_slot_count`の2フィールドを追加した（詳細はConfidence Summary参照）

## Test Results

実行コマンド: `cd atelier && ./addons/gdUnit4/runtest.sh -a res://tests/ -c`（事前に`godot --headless --path atelier --import`でインポートキャッシュを再構築済み）

```
Overall Summary: 516 test cases | 0 errors | 0 failures | 0 flaky | 0 skipped | 0 orphans
Executed test suites: (55/55)
Executed test cases : (516/516)
Exit code: 0
```

本plan着手前（workshop plan検証時点、470 test cases / 52 suites）から **+46 test cases / +3 suites** 増加しており、015〜018で新規追加した以下のテストに対応する。

| テストファイル | 内容 |
|---|---|
| `tests/integration/test_game_state_garden.gd`（既存拡張） | task015: `advance_turn_growth()`の正常系6・異常系2・境界値2 |
| `tests/unit/features/garden/test_plant_slot_view.gd`（新規） | task016: `PlantSlotView`の4状態判定・シグナル発行・境界値 |
| `tests/unit/features/garden/test_seed_inventory_list.gd`（新規） | task017: `SeedInventoryList`の一覧表示・在庫連動 |
| `tests/integration/test_garden_screen.gd`（新規） | task018: `GardenScreen`のシーンレベル統合テスト（scene_runner使用） |

### 015〜018対象コードのテスト充足状況（数え上げ基準、カバレッジ計測機構なしのため代替評価）

| 対象 | 正常系 | 異常系 | 境界値 | 判定 |
|------|-------|-------|-------|------|
| task015: `GameState.advance_turn_growth()` | ○（6件） | ○（庭が空・SeedMaster欠落） | ○（猶予ちょうど vs 超過） | OK |
| task016: `PlantSlotView.setup()`/`setup_empty()`/`status_*()` | ○（6件） | ○（空き・生育中でボタン無効化） | ○（枯死警告閾値の境界） | OK |
| task017: `SeedInventoryList.setup()` | ○（4件） | ○（在庫0・マスター欠落） | ○（空配列） | OK |
| task018: `GardenScreen`（種植え/ターン終了/収穫/枯死通知/ショップ導線/植付失敗/保守性） | ○（5件） | ○（在庫切れ植付失敗） | -（シーン統合テストのため対象外） | OK |

## File Size Check

500行超過ファイル: **1件**（`atelier/autoload/game_state.gd`）

| ファイル | 行数 | 判定 |
|---------|-----|------|
| `atelier/autoload/game_state.gd` | 895 | **超過・既知の受容事項**（後述） |
| `atelier/shared/theme/theme.gd` | 13 | OK |
| `atelier/features/garden/ui/plant_slot_view.gd` | 137 | OK |
| `atelier/features/garden/ui/seed_inventory_list.gd` | 92 | OK |
| `atelier/features/garden/ui/garden_screen.gd` | 141 | OK |
| `atelier/tests/integration/test_game_state_garden.gd` | 188 | OK |
| `atelier/tests/integration/test_garden_screen.gd` | 133 | OK |
| `atelier/tests/unit/features/garden/test_plant_slot_view.gd` | 214 | OK |
| `atelier/tests/unit/features/garden/test_seed_inventory_list.gd` | 140 | OK |

`atelier/autoload/game_state.gd`（895行）は、workshop plan検証時点（828行）から本plan（015・018）の追加分（+67行: `advance_turn_growth()`本体、`get_state()`への2フィールド追加）を加えた結果。`.gdlintrc`の`max-file-lines: 1000`（ハード上限）にはまだ余裕があるが、コーディングスタイルの300行ソフト上限は引き続き大幅超過しており、workshop plan検証レポートで既に指摘済みの通り次plan以降での責務分割リファクタリングを改めて推奨する。

## Confidence Summary

### 🟡 要確認（Medium Confidence）

- **`UiTheme`への庭スロット4状態色4件の新規追加**（`COLOR_SLOT_EMPTY`/`COLOR_SLOT_GROWING`/`COLOR_SLOT_HARVESTABLE`/`COLOR_SLOT_WITHER_WARNING`）: `ui-design/screens/garden.md`が未作成で色コード自体が未確定（`ui-design/overview.md`に明記済み）だったため、タスク016実装のために暫定値を新規決定した。庭フェーズのアクセントカラー（リーフグリーン）を生育中に流用し、他3色は判別用の補助的な位置づけ。専用ビジュアルデザインパス着手時に見直しが必要
- **`PlantSlotView`の状態アイコン**（➕/🌱/🌾/⚠）: 同様に`ui-design/screens/garden.md`未作成のため、NFR-201（色以外でも判別可能）を満たす目的で妥当な推測により暫定決定

### 🔴 要判断（Low Confidence）

- **`GameState.get_state()`への`seed_masters`・`garden_slot_count`フィールド追加**: タスク018のInterfacesには明記されていなかった設計判断。当初は`get_seed_masters()`/`get_garden_slot_count()`という専用アクセサ2件を追加する実装だったが、`gdlint`の`max-public-methods: 20`（`game_state.gd`は追加前時点で既に上限ギリギリだった）に抵触したため、既存の`get_state()`統合Dictionaryへ2フィールドを追加する方式に変更した。`state-management.md`の「単一の情報源」原則には合致するが、他のマスターデータ（`RecipeMaster`, `MaterialMaster`, `UpgradeMaster`等）は`get_state()`に含まれておらず、庭機能のみ例外的にマスターデータを公開する非対称な設計になっている。次plan（alchemy/guild等のUI実装）で同様のニーズが生じた際、この非対称性をどう扱うか（他機能も追随してマスターデータを公開するか、専用アクセサパターンに統一するか）を人間による判断が必要
- **`GardenScreen`のトースト通知実装**（`_show_toast()` / `get_toast_text()`）: Test Strategyには「トースト等のフィードバックが表示される」とのみ記載され、具体的なUI要素・アニメーション・自動消去タイマー等の仕様は未確定だったため、単一`Label`へのテキスト即時反映という最小実装に留めた。本格的なトーストUI（アニメーション・自動消去・スタック表示等）は別途デザインパス・実装タスクが必要

## Recommendations

1. **garden planは18/18タスク完了・全チェックOKでマージ可能な状態にある。** テスト516件全パス、Lint/Format双方OK、500行ルール超過は既存の既知の受容事項（`game_state.gd`）のみ。
2. `game_state.gd`（895行）の肥大化は4plan連続（rank-up→workshop→garden）で進行している。`max-public-methods: 20`に今回抵触しかけたことも踏まえ、次plan着手前に責務分割（例: 庭関連メソッド群を専用クラスへ委譲する等）のリファクタリングIssueを起票することを強く推奨する。
3. `GameState.get_state()`への`seed_masters`/`garden_slot_count`追加は🔴要判断（上記）。次のUI実装plan（alchemy/guild/workshop等）着手前に、マスターデータのUI公開方針を明文化しておくことを推奨する。
4. `atelier/tests/integration/test_recipe_fixture_load.gd`の`gdformat --check`未整形は本plan外（alchemy plan由来、diffなし、workshop plan検証時から継続する既知事項）のため対応不要。

VERIFY_ALL_PASSED
