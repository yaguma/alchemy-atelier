# Verification Report - 2026-09-23
## Plan: ui-polish

## Summary

| Item | Result |
|------|--------|
| Tasks | 18/18 completed |
| Tests | 1337 passed, 0 failed, 0 skipped（3回連続実行で完全一致） |
| Coverage | N/A（GDScript/GdUnit4に計測機構なし、数え上げ基準で代替確認。軽微な穴1件あり、詳細は下記） |
| Build | OK |
| Lint | OK（Plan対象外ファイルに既存警告1件、詳細は下記） |
| 500-line rule | NG: 2 files over |

## Task Status

`docs/dev/plans/ui-polish/tasks/` 配下の全18タスクファイルのfrontmatter `status` を確認。全件 `done`。

| ID | Title | Status |
|----|-------|--------|
| 001 | UiEffects共通演出ヘルパーとUiTheme演出定数を新設する | done |
| 002 | 庭画面で種を植えた時のポップイン演出を追加する | done |
| 003 | 庭画面で収穫時に素材アイコンが在庫へ飛ぶ演出を追加する | done |
| 004 | 庭画面で枯死時のフェードアウト演出を追加する | done |
| 005 | 庭画面のスロット満杯エラーをユーザー向け文言に変換する | done |
| 006 | 調合画面で素材投入時のスライド演出を追加する | done |
| 007 | 調合画面で特性発現時のハイライト演出を追加する | done |
| 008 | 調合画面で調合実行時の完成品生成演出を追加する | done |
| 009 | 調合画面の在庫リストに空状態表示を追加する | done |
| 010 | ギルド納品画面の表示演出（フェードイン+完成品ポップ）を追加する | done |
| 011 | ランクノルマバーの減少アニメーションをguild/rank_hud共通で追加する | done |
| 012 | ギルド納品画面で指定合致時のハイライト演出を追加する | done |
| 013 | 昇格試験結果確定のシグナル配線を中継方式に是正する | done |
| 014 | 昇格試験の成功/失敗結果演出コンポーネントを新設する | done |
| 015 | 昇格試験の試験ノルマ減少演出がタスク011の実装を流用できることを確認する | done |
| 016 | 工房画面で購入成功時のゴールドカウントダウン演出を追加する | done |
| 017 | 工房画面表示時のフェードイン演出を追加する | done |
| 018 | 工房画面で購入不可時に価格テキストを警告色にする | done |

## Test Results

コマンド: `"$GODOT_BIN" --path atelier -s -d --remote-debug tcp://127.0.0.1:0 res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/ -c`
（`runtest.sh`は`cd atelier`後の相対`--path .`実行が前提だが、本セッションのBash呼び出し間ではcwdが永続化されなかったため、Godot本体を絶対パス`--path`で直接起動する等価コマンドに置き換えて実行した）

| Run | Test cases | Errors | Failures | Flaky | Skipped | Orphans | Time |
|-----|-----------|--------|----------|-------|---------|---------|------|
| 1 | 1337 | 0 | 0 | 0 | 0 | 0 | 1min 30s |
| 2 | 1337 | 0 | 0 | 0 | 0 | 0 | 1min 29s |
| 3 | 1337 | 0 | 0 | 0 | 0 | 0 | 1min 30s |

3回とも133/133 test suites、1337/1337 test casesが全てPASSED、結果は完全に一致（exit code 0）。

### trait_highlight flaky調査

`test_alchemy_screen_trait_highlight.gd`（4ケース）および`test_alchemy_slot_view.gd`の`test_play_trait_highlightで有効なTweenが返りSlotPanelのself_modulateが元の色へ戻る`を3回とも個別に確認したところ、いずれの実行でも全ケースPASSEDだった。タスク017実装時に報告された1件の失敗は、今回の検証セッション（3回連続フルスイート実行、Godotエディタ本体の直接起動で実施）では再現しなかった。エラー種別が「1件のみ・他は正常・実装者本人も単体実行では成功と報告」という性質と合わせ、環境依存の一過性flaky（GdUnit4のTween完了待ち`await`とテスト実行機のスケジューリング負荷の競合など）であり、ui-polish Planのコード自体に起因する再現性のある回帰ではないと判断する。ただし3回のみの確認であり完全な断定はできないため、今後同様の失敗が再発した場合はTween完了待ちのタイムアウト余裕（`awaiter`のframe数等）を見直すことを推奨する。

## Coverage

GDScript/GdUnit4に標準カバレッジ計測機構はないため、「`logic/*.gd`および今回新設した`shared/ui/ui_effects.gd`の全public static funcに正常系・異常系・境界値のテストを最低1本ずつ」という数え上げ基準で確認した。

`atelier/shared/ui/ui_effects.gd`（タスク001新設、6 public static func）:

| 関数 | 直接ユニットテスト | 備考 |
|------|------------------|------|
| `play_pop_in` | あり | `test_ui_effects.gd` |
| `play_fade_in` | **なし** | 直接の単体テストが存在しない。ただし`workshop_screen.gd`（タスク017）・`exam_outcome_overlay.gd`（タスク014）・`guild_delivery_screen.gd`（タスク010）の統合テスト3本で間接的に呼び出し経路がカバーされている |
| `fly_ghost` | あり（2ケース、完了後の後片付けも含む） | `test_ui_effects.gd` |
| `play_wither_fade` | あり | `test_ui_effects.gd` |
| `animate_progress_value` | あり | `test_ui_effects.gd` |
| `play_highlight_pulse` | あり | `test_ui_effects.gd` |
| （共通）duration<=0境界値 | あり（2ケース: 0, -1） | `test_ui_effects.gd` |

`play_fade_in`のみ直接の単体テストを欠いており、数え上げ基準を厳密には満たしていない（間接カバーはあるため実質的なリスクは低い）。Recommendationsに記載。

## Build Result

コマンド: `"$GODOT_BIN" --headless --path atelier --export-debug "Windows Desktop" "build/verify_ui_polish.exe"`

OK。`atelier/build/verify_ui_polish.exe`（約117MB、実行可能ファイル属性あり）が生成された。ビルドログにERROR/Failed等の異常終了メッセージなし。

## Lint Result

```
gdlint atelier/features/ atelier/shared/ atelier/autoload/ atelier/scenes/
```

```
atelier/autoload/game_state.gd:1: Error: "Class global scope" has more than 20 public methods (functions) (max-public-methods)
Failure: 1 problem found
```

`PYTHONUTF8=1 PYTHONIOENCODING=utf-8`を付与して再実行しても同一の結果であり、日本語関数名に起因するエンコーディングのfalse positiveではなく実際のルール違反と確認した。ただし`atelier/autoload/game_state.gd`は今回のui-polish Planの変更対象ファイル一覧（git status）に含まれておらず、既存（Plan着手前から存在）の警告である。ui-polish Planによる新規回帰ではないため、本Planのマージをブロックする理由にはならない（別途Issue化を推奨）。

```
gdformat --check atelier/features/ atelier/shared/ atelier/autoload/ atelier/scenes/
```

`86 files would be left unchanged` — OK、フォーマット崩れなし。

## File Size Check

タスク`## Files`セクションに列挙された全ファイル（新規・変更、コード本体+テスト）および、git status上でui-polish Planの一環と見られる追加の変更テストファイルについて行数を確認した。

500行を超えるファイル:

| ファイル | 行数 | 備考 |
|---------|------|------|
| `atelier/features/alchemy/ui/alchemy_screen.gd` | 607 | タスク006/007/008/010/013の累積追加により+124/-6（純増118行）。元々489行で300行ルールも超過していたが、本Planでさらに超過幅が拡大した |
| `atelier/tests/integration/test_main_scene_full_loop_playthrough.gd` | 504 | 元495行から+9/-1。タスク013/014のフェーズ遷移変更に追随した微修正で500行を超えた |

その他の変更・新規ファイル（`ui_effects.gd` 66行、`exam_outcome_overlay.gd` 61行、`main.gd` 376行、各テストファイル最大153行等）はいずれも500行以内。

## Confidence Summary

### 🟡 要確認（Medium Confidence）

- `atelier/shared/theme/theme.gd` L140以降: タスク001が新設した`ANIM_DURATION_*`/`ANIM_EASE_*`系の演出定数群は全てプレースホルダー値。以降のタスク（002〜017）が参照する土台のため、実プレイでの体感調整が今後必要
- `atelier/shared/theme/theme.gd:148` `ANIM_DURATION_GOLD_COUNTDOWN`（タスク016）: カウントダウン演出時間もプレースホルダー
- `atelier/shared/ui/rank_hud.gd`（タスク011）: `animate`フラグの適用範囲をノルマバーのみに限定し、他3要素（ランク名・ゴールド等）は従来通り瞬時反映のまま。意図的なスコープ限定だが、体感の統一感は要確認
- `atelier/features/guild/ui/guild_delivery_screen.gd`（タスク010）: 結果行ポップ演出を「順次」開始させる間隔値がプレースホルダー
- `atelier/features/guild/ui/guild_delivery_screen.gd` / `guild_delivery_result_row.gd`（タスク012）: 指定合致ハイライトの発火タイミング（ポップイン完了後）・GPUParticles2Dの代わりにTween明滅+スケールパルスで代替（ドット絵統一との一貫性を優先したユーザー決定）
- `atelier/features/alchemy/ui/alchemy_slot_view.gd:58`（タスク007）: ハイライト対象を`self`ではなく`_slot_panel`にする実装判断（ルートControl自身は背景を持たないため）
- 各タスクのトースト文言・ラベル書式（例: `CRAFT_RESULT_POPUP_TEXT_FORMAT`等）は概ね暫定文言としてマークされている

### 🔴 要判断（Low Confidence）

- `atelier/scenes/main.gd` L256-266付近（タスク013/014）: `GameState.commit_exam_outcome()`が`exam_outcome_confirmed → game_cleared/game_over`を同一フレーム内で同期発行する既存挙動に対し、タスク014で導入した「画面遷移をacknowledged（プレイヤーのオーバーレイ確認操作）まで遅延させる」設計が絡み合っている。最終ランク成功/ゲームオーバー確定時、`_on_exam_outcome_acknowledged()`が`is_game_cleared()/is_game_over()`を見て遷移を上書きしないよう明示的にガードしている。これは検証依頼で共有された「最終ランク到達時、試験結果オーバーレイがResult画面の上に重ねて表示される仕様（ユーザー確認済み・現状維持で決定済み）」と一致する既知の仕様であり、新規の問題ではない。ただしロジックの結合度が高く、将来`commit_exam_outcome()`側の発行順序が変わった場合は本ガードの再検証が必要
- `atelier/features/alchemy/ui/alchemy_screen.gd`が607行に到達（500行ルール超過）。タスク006/007/008/010/013の5タスクが同一ファイルに演出・シグナル配線を積み増した結果であり、機能的な不具合ではないが保守性の観点でリファクタリング（責務分割）を検討すべき状態

## Issues Found

1. **[Warning] 500行ルール超過（2件）**: `alchemy_screen.gd`（607行）、`test_main_scene_full_loop_playthrough.gd`（504行）。上記File Size Check参照
2. **[Info] gdlint警告1件（Plan対象外）**: `atelier/autoload/game_state.gd`の`max-public-methods`警告。ui-polish Plan着手前から存在する既存の警告で、本Planの変更ファイルには含まれない
3. **[Info] カバレッジの軽微な穴**: `UiEffects.play_fade_in()`に直接の単体テストがない（間接的な統合テストカバーはあり）
4. **[Info] trait_highlightのflaky報告**: 今回の3回連続実行では再現せず。一過性の環境依存flakyと判断（詳細は上記Test Results参照）

## Recommendations

1. `alchemy_screen.gd`（607行）の責務分割を検討する。特にタスク006/008の演出関連コード（スライド・生成ポップ）を専用ヘルパーまたは子コンポーネントへ切り出す余地がある
2. `UiEffects.play_fade_in()`に直接の単体テストを1本追加し、数え上げ基準（全public static funcに最低1本）を完全に満たす
3. `game_state.gd`のmax-public-methods警告は本Planのスコープ外だが、別Issueとして起票し解消を検討する
4. `theme.gd`のプレースホルダー演出定数群（duration/ease/文言）は、実プレイでの人間による体感調整を次のイテレーションで実施する
5. trait_highlightのflaky再発時は、Tween完了待ちの`await`まわり（GdUnitのシグナル待ちタイムアウト等）を優先的に疑う

VERIFY_ISSUES_FOUND
