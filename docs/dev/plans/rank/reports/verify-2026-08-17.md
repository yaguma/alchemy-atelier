# Verification Report - 2026-08-17
## Plan: rank

## Summary

| Item | Result |
|------|--------|
| Tasks | 8/8 completed |
| Tests | 317 passed, 0 failed, 0 skipped（0 errors / 0 flaky / 0 orphans） |
| Coverage | 数え上げ基準（数値目標なし）: OK |
| Build | N/A（エクスポートプリセット未確定） |
| Lint | OK（gdlint: no problems / gdformat: 28 files unchanged） |
| 500-line rule | NG - 1 file over（`atelier/autoload/game_state.gd` 574行） |

## Task Status

| ID | Title | Status |
|----|-------|--------|
| 001 | RankOutcome enumを実装する | done |
| 002 | RankMasterのResource型を実装する | done |
| 003 | GameBalanceにランク進行関連定数を追加する | done |
| 004 | RankState型を実装する | done |
| 005 | RankQuotaResolverを実装する | done |
| 006 | TurnLimitResolverを実装する | done |
| 007 | GameStateにランク関連フィールドを追加しtraits_unlockedをランク由来に置き換える | done |
| 008 | GameStateにランク結果評価・確定処理を実装し納品時のノルマ消費を統合する | done |

## Test Results

実行コマンド: `cd atelier && ./addons/gdUnit4/runtest.sh -a res://tests/ -c`

```
Overall Summary: 317 test cases | 0 errors | 0 failures | 0 flaky | 0 skipped | 0 orphans
Executed test suites: (38/38)
Executed test cases : (317/317)
Total execution time: 1min 10s 834ms
Exit code: 0
```

直近実行と同じく317件全PASSを再確認した。失敗テストなし。

### 本Plan該当テストの内訳（Test Strategy チェックリストとの突合）

| タスク | テストファイル | ケース数 | Test Strategy カバー状況 |
|---|---|---|---|
| 001 | `atelier/tests/unit/features/rank/test_rank_outcome.gd` | 5 | OK（3値定義・一意性に加え、未定義値の非存在も検証） |
| 002 | `atelier/tests/unit/features/rank/test_rank_master.gd` | 7 | OK（7フィールド読み書き / フィクスチャのみでのインスタンス化 / `exam_*` デフォルト0の明示検証） |
| 003 | `atelier/tests/unit/shared/test_game_balance_rank.gd` | 4 | OK（`MAX_DEMOTION_COUNT == 3` / `INITIAL_RANK_ID == &"rank_g"` と型検証） |
| 004 | `atelier/tests/unit/features/rank/test_rank_state.gd` | 3 | OK（デフォルト値 / clone一致 / clone独立性） |
| 005 | `atelier/tests/unit/features/rank/test_rank_quota_resolver.gd` | 12 | OK（減算・0クランプ・0からの減算・同値減算 / `is_rank_cleared` 正常＋負値 / `reset_for_retry` 正常・引数不変・null安全・インスタンス独立・純粋性） |
| 006 | `atelier/tests/unit/features/rank/test_turn_limit_resolver.gd` | 8 | OK（等価/未満/超過 の3境界 / `resolve_rank_outcome` 4組合せ / 純粋性） |
| 007 | `atelier/tests/integration/test_game_state_rank_foundation.gd` | 14 | OK（初期値 / `get_state()`キー / 防御的コピー2種 / フォールバック2種 / `traits_unlocked` 解禁・未解禁の対比 / `_set_traits_unlocked_for_test` 不在） |
| 008 | `atelier/tests/integration/test_game_state_rank_outcome.gd` | 23 | OK（ノルマ減算4種＋空キュー / `_accumulated_contribution` 不在 / `evaluate` 3結果＋副作用なし / DEMOTION確定 / ゲームオーバー境界 / 冪等性2種 / シグナル4種 / PROMOTION_ELIGIBLE時の非変化） |

補足: タスク007の「`execute_alchemy()`へのtraits_unlocked伝播」はalchemy plan側の
`atelier/tests/integration/test_game_state_execute_alchemy.gd`（解禁/未解禁の対比2ケース）でも
ランク注入経由に更新済み。タスク008の納品時ノルマ消費は
`atelier/tests/integration/test_game_state_deliver_pending_products.gd`（2ケース）でも重ねて検証されている。

## Architecture Consistency Check（本Plan特有）

| # | 確認項目 | 結果 |
|---|---|---|
| 1 | `game_state.gd` に `_traits_unlocked` / `_set_traits_unlocked_for_test()` が存在しない（タスク007で削除） | OK — 実装側にヒット0件。ヒットは削除を検証するテスト（`test_game_state_rank_foundation.gd:161-162`）のみ |
| 2 | `game_state.gd` に `_accumulated_contribution` が存在しない（タスク008で削除） | OK — 実装側にヒット0件。ヒットは削除を検証するテスト（`test_game_state_rank_outcome.gd:117-118`）のみ |
| 3 | `deliver_pending_products()` が `RankQuotaResolver.apply_contribution` 経由で `_rank_state.quota` を更新 | OK — `atelier/autoload/game_state.gd:283-285` にて `_rank_state.quota = RankQuotaResolver.apply_contribution(_rank_state.quota, delivery_result.final_contribution)` |
| 4 | `_current_rank_id` / `_rank_state` / `_demotion_count`（＋`_rank_masters`）が存在 | OK — `atelier/autoload/game_state.gd:45-48`。`reset_for_test()`（L570-573）でも初期化済み |

追加で確認した点:

- `execute_alchemy()` の traits_unlocked 参照は `atelier/autoload/game_state.gd:235` の
  `_get_current_rank_master_or_fallback().traits_unlocked` に一本化され、
  `QualityCalculator.calculate_quality` と `TraitActivation.resolve_traits` の双方へ同一値が渡っている（FR-104/FR-201）。
- フォールバックは `atelier/autoload/game_state.gd:349-356` で `push_error()` 後に
  `traits_unlocked = false` の安全側 `RankMaster` を返す（CON-008準拠、クラッシュしない）。
- CON-006（`.tres`実データを作らない）は維持されている。`atelier/data/ranks/` は `.gitkeep` のみ。
  `test_rank_master.gd` の `.tres` ラウンドトリップ検証は `user://test_rank_master.tres` を使い
  `after_test()` で削除するため、リポジトリへの実データ混入はない。
- Domain層（`features/rank/logic/`）は `GameState` を参照しておらず、Functional Core/Imperative Shell 分離は保たれている。

## File Size Check

500行超過（警告）:

| ファイル | 行数 |
|---|---|
| `atelier/autoload/game_state.gd` | 574 |

500行以内（OK）:

| ファイル | 行数 |
|---|---|
| `atelier/features/rank/logic/rank_outcome.gd` | 8 |
| `atelier/features/rank/resources/rank_master.gd` | 16 |
| `atelier/features/rank/state/rank_state.gd` | 16 |
| `atelier/features/rank/logic/turn_limit_resolver.gd` | 21 |
| `atelier/features/rank/logic/rank_quota_resolver.gd` | 29 |
| `atelier/shared/constants/game_balance.gd` | 64 |
| `atelier/tests/unit/shared/test_game_balance_rank.gd` | 17 |
| `atelier/tests/unit/features/rank/test_rank_state.gd` | 33 |
| `atelier/tests/unit/features/rank/test_rank_outcome.gd` | 40 |
| `atelier/tests/unit/features/rank/test_turn_limit_resolver.gd` | 59 |
| `atelier/tests/unit/features/rank/test_rank_master.gd` | 103 |
| `atelier/tests/unit/features/rank/test_rank_quota_resolver.gd` | 106 |
| `atelier/tests/integration/test_game_state_rank_foundation.gd` | 210 |
| `atelier/tests/integration/test_game_state_execute_alchemy.gd` | 260 |
| `atelier/tests/integration/test_game_state_rank_outcome.gd` | 291 |
| `atelier/tests/integration/test_game_state_deliver_pending_products.gd` | 291 |

## Confidence Summary

### 🟡 要確認（Medium Confidence）

- [shared/constants/game_balance.gd:61](../../../../atelier/shared/constants/game_balance.gd) — `MAX_DEMOTION_COUNT := 3`。`spec/requirements.md §7` の「仮3」をそのまま採用した暫定バランス値。プレイテストで妥当性を確認する必要がある（CON-007）
- [autoload/game_state.gd:467,476,487,497](../../../../atelier/autoload/game_state.gd) — テスト専用API群（`_set_rank_masters_for_test` / `_set_current_rank_id_for_test` / `_set_rank_state_for_test` / `_set_demotion_count_for_test`）。既存パターン踏襲の新規補完（FR-301）。いずれも `assert(OS.is_debug_build())` ＋ `push_error()` の二重ガード実装済みであることは確認済み
- [features/rank/state/rank_state.gd](../../../../atelier/features/rank/state/rank_state.gd) — `RefCounted` 継承の判断（CON-011）。`GardenState`/`SlotState` と同型パターンで一貫している

### 🔴 要判断（Low Confidence）

- [shared/constants/game_balance.gd:64](../../../../atelier/shared/constants/game_balance.gd) — `INITIAL_RANK_ID := &"rank_g"`。本plan内での新規補完。対応する `res://data/ranks/rank_g.tres` は未作成のため、実データ投入plan（FR-405）でID体系の整合を取る必要がある
- [features/rank/logic/rank_outcome.gd:2](../../../../atelier/features/rank/logic/rank_outcome.gd) — `features/rank/logic/` への配置判断（CON-003）。設計クラス図に配置未確定だった箇所を本plan内で決定
- [autoload/game_state.gd:346,362,373](../../../../atelier/autoload/game_state.gd) — `_get_current_rank_master_or_fallback()` のフォールバック具体値（CON-008）、および `evaluate_rank_outcome` / `commit_rank_outcome` の2メソッド分離設計（CON-009）。いずれも本plan内の新規決定
- [autoload/game_state.gd:15-16](../../../../atelier/autoload/game_state.gd) — `rank_outcome_confirmed` / `game_over` シグナル（FR-112/FR-113）。購読側UIが本plan外のため、シグナル引数の形が実UI要件に合うかは未検証
- [autoload/game_state.gd:49](../../../../atelier/autoload/game_state.gd) — `_last_rank_outcome` フィールド。ゲームオーバー後の `commit_rank_outcome()` 冪等返却（FR-202）のために追加された、タスク定義に明記のなかった補完フィールド
- [features/rank/logic/rank_quota_resolver.gd:13](../../../../atelier/features/rank/logic/rank_quota_resolver.gd) — 負値ノルマも「クリア済み」扱いとする防御的仕様
- [features/rank/logic/turn_limit_resolver.gd:13](../../../../atelier/features/rank/logic/turn_limit_resolver.gd) — 早期クリアボーナス（FR-411）を保証する早期リターン構造

> 🔴 が7項目残存している。いずれも実装内容そのものは設計文書（core-systems.md / data-schema.md）および
> 本planの制約（CON-003/008/009）と整合しており、テストでも検証済みだが、
> **設計文書に典拠がなく本plan内で新規決定した箇所**であるため、PRレビューでの人間の確認を推奨する。

## Issues Found

1. **[Warning] 500行ルール超過: `atelier/autoload/game_state.gd`（574行）**
   garden / alchemy / guild / rank の4featureぶんの Imperative Shell が単一Autoloadに集約された結果、
   プロジェクト規約（`.claude/rules/coding-style.md`「300行を超えたら分割を検討」、および本検証の500行ルール）
   を超過している。本planの実装が原因というより、feature追加のたびに単調増加している構造的な問題。
   機能上の不具合はなく、テスト・Lintはすべてパスしている。

2. **[Info] 新規統合テスト2ファイルに `.uid` が生成されていない**
   `atelier/tests/integration/test_game_state_rank_foundation.gd` と
   `atelier/tests/integration/test_game_state_rank_outcome.gd` は `.uid` が未生成（他の新規 `.gd` には存在する）。
   テスト実行には影響しないが、コミット前に `godot --headless --path atelier --import` で
   インポートを完走させ、`.uid` を揃えることを推奨する。

3. **[Info] 本planの成果物が未コミット**
   `git status` 上、rank関連の実装・テストはすべて未コミット（新規ファイルは untracked）。
   あわせて `test_game_state_alchemy_foundation.gd` / `test_game_state_guild_foundation.gd` /
   `test_game_state_execute_alchemy.gd` / `test_game_state_deliver_pending_products.gd` が変更されている。
   これは `_traits_unlocked` / `_accumulated_contribution` 撤去に伴う既存テストの更新であり、
   NFR-303が明示的に許容する破壊的変更にあたる。コミットメッセージへの理由記載（タスク007注意事項）を忘れないこと。

## Recommendations

1. `game_state.gd` の分割を検討する。ただし `GameState` はAutoloadであり単一シングルトンである必要があるため、
   単純なファイル分割はできない。現実的な選択肢は以下:
   - (a) 本planでは受容し、分割検討を独立したリファクタリングIssueとして起票する（推奨）
   - (b) feature単位の内部ヘルパー（例: `autoload/game_state_rank_ops.gd` の `static func` 群）へ
     ロジックを切り出し、`GameState` からは委譲のみ行う
   後続の promotion-exam / workshop planでさらに肥大化する見込みのため、**(a) で今回はマージし、
   次plan着手前にリファクタリングを実施する**のが妥当と考える。
2. コミット前に `godot --headless --path atelier --import` を実行し、新規テスト2ファイルの `.uid` を生成する。
3. 🔴 7項目のうち、特に `INITIAL_RANK_ID`（実データ未作成）と
   `evaluate_rank_outcome`/`commit_rank_outcome` の2メソッド分離設計は、
   PRレビューで人間の確認を得ることを推奨する。
4. `MAX_DEMOTION_COUNT = 3` は暫定値のため、`balance-design.md` のTBD項目として引き続き追跡する。
