# Verification Report - 2026-08-30
## Plan: integration-playthrough-verify

## Summary

| Item | Result |
|------|--------|
| Tasks | 5/5 completed |
| Tests | 856 passed, 0 failed, 0 skipped（フルスイート、79スイート） |
| Coverage | 数値目標なし（logic/の全public static funcに正常系・異常系・境界値テスト最低1本ずつが基準。本Planはlogic/の新規変更を伴わないUI結合テストのため対象外） |
| Build | N/A（エクスポートビルド未構築） |
| Lint | OK（`gdlint features/ shared/ autoload/`で警告なし） |
| Format | OK（`gdformat --check`で新規ファイルのフォーマット崩れなし） |
| 500-line rule | OK（新規ファイル478行） |

## Task Status

| ID | Title | Status |
|----|-------|--------|
| 001 | ロングランプレイスルーテストの雛形と共通フィクスチャヘルパーを実装する | done |
| 002 | G→F昇格までの通常ターン〜試験合格シナリオを実装する | done |
| 003 | 工房強化のUI購入とGameState反映を検証するステップを実装する | done |
| 004 | F→E昇格サイクルと通し全体の非再生成アサーションを実装する | done |
| 005 | 全体通し実行・静的解析・フォーマットチェックを行い検証レポートをまとめる | done |

## Test Results

全テストスイート（`res://tests/`配下79スイート、856件）が0 errors / 0 failures / 0 flaky / 0 skipped / 0 orphansでPASSED。

新規追加ファイル`atelier/tests/integration/test_main_scene_full_loop_playthrough.gd`（478行、7テストケース）:

- `test_フィクスチャ注入後にGランクから開始できる`: PASSED
- `test_MainSceneと4画面のインスタンスIDを観測できる`: PASSED
- `test_G昇格試験に合格しworkshop画面へ自動遷移する`: PASSED
- `test_G昇格後の工房で恒久強化を購入すると反映され庭へ復帰する`: PASSED
- `test_ゴールド不足の状態で恒久強化を購入しても状態が変化しない`: PASSED
- `test_ランクを跨いで再度入場してもランク状態がリセットされる`: PASSED
- `test_G昇格から工房強化購入を経てF昇格まで実機シーングラフのみで通しで到達する`（Plan中核シナリオ）: PASSED

main-scene-integration Plan完了時点（797件）→daily-orders Plan完了時点（849件）から7件増加（856件）。

## Coverage Results

GDScript/GdUnit4に%カバレッジ計測機構が無いため数値目標は不採用。本Planは既存のUI（`MainScene`・`GardenScreen`・`AlchemyScreen`・`WorkshopScreen`）と既存のGameState Domain層を「実機シーングラフ越しの結合」として検証するものであり、`logic/`配下への新規変更は行っていない。CLAUDE.md「次のステップ」に記載の「5機能を通した結合プレイ（庭→調合→ギルド納品→ランク進行→工房強化のループが実機で通しで回るか）」の未検証ギャップは、本Planの追加テスト（特にタスク004の`test_G昇格から工房強化購入を経てF昇格まで実機シーングラフのみで通しで到達する`）により解消されたと判断する。

## File Size Check

新規ファイルは478行で500行ルール以内。既存の変更ファイルなし（新規ファイル1件のみ）。

## Confidence Summary

### 🟡 要確認（Medium Confidence）

- なし（タスク003・004で発見された挙動はすべて調査済みで🔵として本文コメントに固定されている。下記Issues Found参照）

### 🔴 要判断（Low Confidence）

- なし

## Issues Found

実装中に本番コード側の以下2点の挙動が判明した。いずれもバグではなく既存の意図した設計と確認できたため、本番コードの修正は行わず、テスト側のコメントとして記録するに留めた。

1. **工房強化画面のCloseButtonの復帰先**: 試験合格による自動遷移（`_on_exam_outcome_confirmed()`が`set_phase(PHASE_WORKSHOP)`を直接呼ぶ経路）でworkshopへ入った場合、CloseButton押下後の復帰先はalchemyではなく`garden`になる。`MainScene._phase_before_workshop`はShopButton経由の入場（`_on_shop_requested()`）でのみ更新されるため、試験合格経由では初期値の庭が残る。`main.gd`の`_on_workshop_closed()`側コメント（AC-004異常系）と一致する仕様であり、Plan当初の想定（「alchemyへ復帰する」）を「庭へ復帰する」へテスト側で修正して固定した（タスク003）。
2. **昇格時のRankState初期化**: ランク昇格が確定すると、本番コード（`game_state_rank_delegate.gd`の`RankQuotaResolver.reset_for_retry()`呼び出し箇所）が新ランクの`quota_max`へ`quota`をリセットし`elapsed_turn`を0へ戻すことをタスク004で確認した。テスト側の`_enter_rank()`による明示的な低ノルマ再注入は、この本番の初期化を単に上書きするだけであり、二重初期化による不整合は発生しない（Plan当初の🟡懸念は解消し🔵へ格上げ）。

## Recommendations

1. 本Planにより「G→F→Eの2段階連続昇格＋工房強化購入」の実機結合検証が自動テストとして確立された。CLAUDE.md「次のステップ」の該当項目を更新することを推奨する。
2. コミット・PR作成（`/commit-push-pr`相当）と`pr-review-team`によるレビューへ進む。

VERIFY_ALL_PASSED
