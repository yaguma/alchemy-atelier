# Verification Report - 2026-08-14
## Plan: alchemy

## Summary

| Item | Result |
|------|--------|
| Tasks | 11/11 completed |
| Tests | 180 passed, 0 failed, 0 skipped（0 errors / 0 flaky / 0 orphans、24スイート） |
| Coverage | 数え上げ基準: 15/15 関数がカバー済み（うち1件は実装上異常系分岐なし。下記注記参照） |
| Build | N/A（本Plan範囲ではビルド対象なし） |
| Lint | OK（gdlint: no problems found / gdformat --check: 20 files unchanged） |
| 500-line rule | OK（最大 394行 = `game_state.gd`） |

検証範囲はタスク007〜011だが、タスクディレクトリ内の001〜011全件のstatusを確認した。

## Task Status

| ID | Title | Status |
|----|-------|--------|
| 001 | ProductInstance型を実装する | done |
| 002 | RecipeMasterマスターデータ型を実装する | done |
| 003 | GameBalanceに調合関連定数を追加する | done |
| 004 | SlotState型とcan_executeを実装する | done |
| 005 | QualityCalculatorを実装する | done |
| 006 | TraitActivationを実装する | done |
| 007 | ProductValueCalculatorを実装する | done |
| 008 | 初期解禁レシピのマスターデータフィクスチャを作成する | done |
| 009 | MasterDataLoaderにrecipesカテゴリ対応を追加する | done |
| 010 | GameStateに調合関連フィールド・マスターデータロード・テストAPIを追加する | done |
| 011 | GameState.execute_alchemyを実装する | done |

## Test Results

実行コマンド: `cd atelier && ./addons/gdUnit4/runtest.sh -a res://tests/ -c`

```
Overall Summary: 180 test cases | 0 errors | 0 failures | 0 flaky | 0 skipped | 0 orphans
Executed test suites: (24/24)
Executed test cases : (180/180)
Total execution time: 40s 497ms
Exit code: 0
```

失敗テストなし。今回範囲の新規テストスイート:

| テストファイル | 行数 | 対象タスク |
|---|---|---|
| `atelier/tests/unit/features/alchemy/test_product_value_calculator.gd` | 158 | 007 |
| `atelier/tests/integration/test_recipe_fixture_load.gd` | 37 | 008 |
| `atelier/tests/integration/test_master_data_loader_recipes.gd` | 87 | 009 |
| `atelier/tests/integration/test_game_state_alchemy_foundation.gd` | 130 | 010 |
| `atelier/tests/integration/test_game_state_execute_alchemy.gd` | 248 | 011 |

## Coverage Results（数え上げ基準）

### `atelier/features/alchemy/logic/product_value_calculator.gd`

| 関数 | 正常系 | 異常系 | 境界値 | 判定 |
|------|--------|--------|--------|------|
| `calculate_contribution` | OK（10.0×1.5×1.3=19.5） | OK（引数3個限定のシグネチャ検証＝FR-406二重乗算防止） | OK（基礎値0→0.0） | OK |
| `calculate_reward` | OK（5.0×1.5×1.0=7.5） | OK（同上） | OK（基礎値0→0.0） | OK |
| `resolve_contribution_bonus` | OK（乗算合成／報酬系タグ無視／混在分離） | OK（空配列→1.0、未登録タグ→1.0） | OK（貢献度系タグ全種の積） | OK |
| `resolve_reward_bonus` | OK（乗算合成／貢献度系タグ無視） | OK（空配列→1.0、未登録タグ→1.0） | OK（報酬系タグ全種の積） | OK |
| `_multiply_bonus`（private） | 上記4関数経由で全分岐到達 | 同左 | 同左 | OK（間接） |

タスク007のTest Strategy 7項目はすべて実テストとして存在する。特にFR-406（指定調合物ボーナスの二重乗算バグ再発防止）は、`get_script_method_list()`で引数名配列を実際に検証する形で担保されており、リグレッション検知として有効。

### `atelier/shared/loaders/master_data_loader.gd`

| 関数 | 正常系 | 異常系 | 境界値 | 判定 |
|------|--------|--------|--------|------|
| `load_all` | OK（recipes全件RecipeMaster／materials回帰: SeedMaster 2件+MaterialMaster 2件） | OK（未知カテゴリ→空配列） | OK（recipesに非RecipeMaster混在時のフィルタ／materials結果にRecipeMaster非混入） | OK |
| `validate_references` | OK（実データでtrue） | OK（未解決produces_material_id→false） | OK（空配列→true） | OK |
| `_resolve_dir_path`（private） | materials/recipes分岐 | 未知カテゴリのデフォルト分岐 | — | OK（間接） |
| `_is_allowed_type`（private） | 両カテゴリの受理型 | 混在リソース拒否テストで到達 | — | OK（間接） |

タスク009のTest Strategy 4項目はすべてカバー済み。

### `atelier/autoload/game_state.gd`（タスク010・011の追加分）

| 関数 | 正常系 | 異常系 | 境界値 | 判定 |
|------|--------|--------|--------|------|
| `execute_alchemy` | OK（貢献度/報酬の式検証、素材消費、pending_products追加+signal、recipe_id一致、特性未解禁/解禁の対比） | OK（`unknown_recipe_id` / `recipe_not_unlocked` / `material_not_owned` / `duplicate_material_in_slot` / `slot_execution_invalid` の5種すべて＋各ケースで状態不変を検証） | OK（投入枠ちょうど4件は成功／5件と0件は失敗） | OK |
| `load_alchemy_master_data` | OK（実データから`recipe_healing_potion`をロード） | 分岐なし（下記注記） | OK（複数回呼び出しで件数が増えない） | OK（注記あり） |
| `get_state`（調合フィールド拡張分） | OK（`pending_products`/`unlocked_recipe_ids`キーの存在と初期値） | OK（戻り値への`append`が内部状態を汚染しないことを両フィールドで検証＝FR-403/AC-012） | — | OK |
| `_validate_alchemy_request`（private） | 検証通過パス | 4種のerror_codeすべてに到達 | — | OK（間接） |
| `_resolve_inventory_indices`（private） | 解決成功パス | 未所持id（-1）パス | — | OK（間接） |
| `_find_inventory_index`（private） | 一致パス | 不一致（-1）パス | — | OK（間接） |
| `_has_duplicate_ids`（private） | 重複なしパス | 重複ありパス | — | OK（間接） |
| `reset_for_test`（調合フィールド拡張分） | OK（5フィールドすべての初期値検証） | — | OK（複数回呼び出し） | OK |

> **注記（`load_alchemy_master_data`）**: 本関数は`load_garden_master_data`と異なり`validate_references`によるエラー分岐を持たないため（レシピ側にID相互参照がない）、異常系テストが存在しないのは実装として妥当。数え上げ基準の趣旨（未検証分岐を残さない）は満たしている。

タスク011のTest Strategy 13項目はすべて実テストとして存在することを照合済み。AC-015が要求する「5種類のerror_codeごとにシグナル発行＋状態不変」も5種すべてで検証されている。

## File Size Check

500行超過ファイルなし。

| ファイル | 行数 |
|---|---|
| `atelier/autoload/game_state.gd` | 394 |
| `atelier/shared/loaders/master_data_loader.gd` | 67 |
| `atelier/shared/constants/game_balance.gd` | 50 |
| `atelier/features/alchemy/logic/product_value_calculator.gd` | 41 |
| `atelier/data/recipes/recipe_healing_potion.tres` | 10 |

`game_state.gd`（394行）は`coding-style.md`の「300行を超えたら分割を検討」の目安を超過している。現時点でgarden/alchemyの2ドメインが1ファイルに同居しており、後続Plan（guild納品・rank進行）の追加でさらに増加が見込まれる。プロジェクトルールの500行上限には未達のため本サイクルではブロッカーとしないが、次Plan着手前に分割方針（例: Autoloadは薄い委譲層とし、ドメインごとのサービスへ処理を移す）を検討することを推奨する。

## Confidence Summary

### 🟡 要確認（Medium Confidence）

- [`product_value_calculator.gd`](../../../../atelier/features/alchemy/logic/product_value_calculator.gd) L3-4 — 特性ボーナスの系統別（貢献度系/報酬系）乗算合成API（`resolve_contribution_bonus`/`resolve_reward_bonus`）は`core-systems.md`のクラス図に存在しない追加分。設計文書側への反映（同期）が未実施。
- [`product_value_calculator.gd`](../../../../atelier/features/alchemy/logic/product_value_calculator.gd) L35 — `_multiply_bonus`が「テーブル未登録タグを黙って無視して等倍扱い」する仕様。タイポしたタグIDがバランス上の無効化として静かに通過するため、マスターデータ拡充時に検知手段がない点は要判断。
- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L282, L314 — `_set_masters_for_test`/`_set_recipe_masters_for_test`（CON-005対応のテスト専用API）。
- [`trait_activation.gd`](../../../../atelier/features/alchemy/logic/trait_activation.gd) L37 — タグ集合の出現順保持仕様。

### 🔴 要判断（Low Confidence）

**`game_balance.gd` の調合バランス定数（すべてAI推論補完、人間による確定が必要）**

- [`game_balance.gd`](../../../../atelier/shared/constants/game_balance.gd) L32 — `ALCHEMY_SLOT_COUNT_DEFAULT := 4`（FR-005）
- [`game_balance.gd`](../../../../atelier/shared/constants/game_balance.gd) L35 — `QUALITY_MULTIPLIER_TABLE := {1:1.0, 2:1.25, 3:1.5, 4:1.75, 5:2.0}`（FR-005）
- [`game_balance.gd`](../../../../atelier/shared/constants/game_balance.gd) L38 — `TRAIT_ACTIVATION_THRESHOLD := 2`（FR-005）
- [`game_balance.gd`](../../../../atelier/shared/constants/game_balance.gd) L41 — `CATALYST_BASE_QUALITY_SCORE := 3`（FR-005）。**本Plan内では未参照の前方定義**であり、WorkshopSystem実装時まで実使用されない
- [`game_balance.gd`](../../../../atelier/shared/constants/game_balance.gd) L44 — `TRAIT_CONTRIBUTION_BONUS := {holy:1.3, purify:1.3, heal:1.3}`（FR-005）
- [`game_balance.gd`](../../../../atelier/shared/constants/game_balance.gd) L47 — `TRAIT_REWARD_BONUS := {gold:1.3, glamour:1.3, rare:1.3}`（FR-005）
- [`game_balance.gd`](../../../../atelier/shared/constants/game_balance.gd) L22 — `WITHER_WARNING_REMAINING_TURNS := 1`（FR-204、設計時点で未確定だった値の補完）

これら7定数は`balance-design.md`の🟡TBD項目に相当し、すべて仮値。特性ボーナスが貢献度系・報酬系ともに一律1.3である点は、コンセプト（v7.0）が謳う「品質を盛るか特性を宿すか」のトレードオフに実質的な差を生まないため、プレイテストによる差別化が必要になる可能性が高い。

**`game_state.gd` の execute_alchemy 周辺の推論補完**

- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L9-10 — `execute_alchemy_failed` signal（gardenの`plant_seed_failed`/`harvest_failed`パターン踏襲、FR-113）
- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L30 — `_pending_products`（FR-008 ギルド納品待ちキュー）
- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L33-34 — `_traits_unlocked`（CON-007。特性解禁は本来RankMaster側で管理する想定だが未実装のため暫定フィールド）
- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L59-60 — `unlocked_recipe_ids`の浅い複製判断（要素がStringName＝値型相当のため、FR-403の防御的コピー要件を満たすと判断）
- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L91 — `load_alchemy_master_data`の**BootSceneからの呼び出し配線が本Plan外**
- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L226-227 — `remove_at`のインデックスずれ回避のため降順削除
- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L231-233 — `_pending_products`へ`clone()`した独立コピーを格納する方針（`harvest()`と同方針）
- [`game_state.gd`](../../../../atelier/autoload/game_state.gd) L292, L304, L325, L337, L349, L360 — テスト専用API群（`_set_seed_inventory_for_test` / `_inject_plant_for_test` / `_set_unlocked_recipe_ids_for_test` / `_set_alchemy_slot_count_for_test` / `_set_traits_unlocked_for_test` / `_inject_material_for_test`）

テスト専用API群はいずれも`assert(OS.is_debug_build(), ...)` + `if not OS.is_debug_build(): push_error(); return` の二重ガードを一貫して備えており、本番コードパスからの誤用に対する防御は適切。

## Issues Found

ブロッカーとなる問題は検出されなかった。以下は次サイクルで対応を検討すべき軽微な事項。

1. **BootSceneがマスターデータロードを呼んでいない（Info、Plan範囲外の既知事項）**
   `atelier/scenes/boot.gd` L11 は依然として `MasterDataLoader.validate_references([])` の空配列固定であり、`load_garden_master_data()` / `load_alchemy_master_data()` のいずれも呼ばれていない。タスク010が「BootSceneからの呼び出し配線自体は本plan外」と明示しているため本サイクルでは仕様どおりだが、実プレイ時に`_recipe_masters`が空のままとなり`execute_alchemy`が必ず`unknown_recipe_id`で失敗する。UI実装Planの前に配線タスクが必要。

2. **テストが実プロジェクトディレクトリへ一時ファイルを書き込む（Info）**
   `test_master_data_loader_recipes.gd` の混在フィルタ検証は `res://data/recipes/_tmp_non_recipe.tres` を実際に生成・削除する。`after_test()` で`.tres`と`.uid`の両方を確実に除去しており（テスト失敗時も動作する配置）、実行後の`git status`にも残留はなかったため実害は確認されていない。ただしテスト中断時にリポジトリが汚れる余地は残る。

3. **一部の新規テストファイルに`.uid`が生成されていない（Info）**
   `test_game_state_alchemy_foundation.gd` / `test_game_state_execute_alchemy.gd` / `test_master_data_loader_recipes.gd` に対応する`.uid`が未生成（他の新規`.gd`には存在する）。テスト実行は全件成功しているため機能影響はないが、コミット前にGodotエディタでインポートを完了させ、生成物を含めてコミットするかどうかを揃えることを推奨する。

4. **`game_state.gd`が394行（Info）**
   上記「File Size Check」参照。プロジェクトの500行上限には未達だが、`coding-style.md`の300行分割検討ラインを超過。

## Recommendations

1. 本サイクルの成果物はテスト・Lint・フォーマット・ファイルサイズのすべてでゲートを通過している。コミット・PR作成に進んで差し支えない。
2. コミット前に、上記Issue 3の`.uid`未生成をGodotエディタでのインポート実行により解消し、`git status`の未追跡ファイル一覧を確認すること。
3. 次Plan（UI実装またはギルド納品）の着手前に、BootSceneからのマスターデータロード配線（Issue 1）をタスク化すること。これが未対応のままではUI経由の調合が実行不能。
4. 🔴の調合バランス定数7件は、`balance-design.md`のTBD解消とプレイテストによる確定が必要。特に特性ボーナスが貢献度系・報酬系とも一律1.3である点は、コンセプトが要求するトレードオフの体感差を生まない懸念があるため、プロトタイプ検証時の重点確認項目とすること。
5. `game_state.gd`の分割方針（Issue 4）を次Plan着手前に検討すること。
