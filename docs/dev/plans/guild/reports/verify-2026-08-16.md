# Verification Report - 2026-08-16
## Plan: guild

## Summary

| Item | Result |
|------|--------|
| Tasks | 6/6 completed |
| Tests | 229 passed, 0 failed, 0 skipped（うちguild関連47件） |
| Coverage | N/A（数え上げ基準で代替、Step3参照。全36項目カバー済み） |
| Build | N/A（エクスポートプリセット未確定） |
| Lint | OK |
| 500-line rule | OK（最大461行 / game_state.gd） |

## Task Status

| ID | Title | Status |
|----|-------|--------|
| 001 | DeliveryResult型を実装する | done |
| 002 | DailyOrderMasterのResource型を実装する | done |
| 003 | GameBalanceにギルド納品関連定数を追加する | done |
| 004 | DeliveryResolverを実装する | done |
| 005 | GameStateにギルド納品関連フィールド・テストAPIを追加する | done |
| 006 | GameState.deliver_pending_productsを実装する | done |

## Test Results

実行コマンド: `./addons/gdUnit4/runtest.sh -a res://tests/ -c`（`atelier/`をカレントディレクトリとして実行）

```
Overall Summary: 229 test cases | 0 errors | 0 failures | 0 flaky | 0 skipped | 0 orphans
Executed test suites: (30/30)
Executed test cases : (229/229)
Total execution time: 51s 400ms
Exit code: 0
```

失敗テストなし。orphan（未解放オブジェクト）検出も0件であり、`monitor_signals(GameState, false)`の第2引数指定漏れによるAutoload誤解放（本プロジェクトで過去に発生した既知の障害パターン）は発生していない。

### guild関連テストファイルの内訳

| テストファイル | テストケース数 |
|---|---|
| `atelier/tests/unit/features/guild/test_delivery_result.gd` | 4 |
| `atelier/tests/unit/features/guild/test_daily_order_master.gd` | 7 |
| `atelier/tests/unit/features/guild/test_delivery_resolver.gd` | 13 |
| `atelier/tests/unit/shared/test_game_balance_guild.gd` | 2 |
| `atelier/tests/integration/test_game_state_guild_foundation.gd` | 8 |
| `atelier/tests/integration/test_game_state_deliver_pending_products.gd` | 13 |
| **合計** | **47** |

## Coverage（数え上げ基準）

各タスクのTest Strategyチェックボックスと実テストの対応を確認した。**全36項目がテストコードに反映済み**。

| タスク | Test Strategy項目数 | 実テスト数 | 充足 |
|---|---|---|---|
| 001 DeliveryResult | 3 | 4 | ✅ 全項目カバー（+ゼロ値ケース追加） |
| 002 DailyOrderMaster | 3 | 7 | ✅ 全項目カバー |
| 003 GameBalance定数 | 2 | 2 | ✅ 全項目カバー |
| 004 DeliveryResolver | 11 | 13 | ✅ 全項目カバー（+引数非破壊性テスト追加） |
| 005 GameState基盤 | 4 | 8 | ✅ 全項目カバー（+シグナル宣言/null再設定テスト追加） |
| 006 deliver_pending_products | 13 | 13 | ✅ 全項目カバー |

特筆すべき充足確認:

- **AC-009 四捨五入境界**: `test_報酬の四捨五入境界で切り捨てと切り上げが分かれる` が `test_parameters` で `[6.4→6]` `[6.5→7]` `[0.0→0]` の3ケースを網羅。タスク006のTest Strategy「`final_reward = 0.0` → `_gold`が変化しない」も同パラメータ化テスト内で充足している。
- **AC-013 純粋性**: `test_同一引数で複数回呼び出しても同じ結果を返す` および `test_解決処理は引数の調合物インスタンスを書き換えない` の2本で担保。
- **AC-015 防御的コピー**: `test_get_stateのpending_productsを変更しても内部キューは汚染されない` で `get_state()["pending_products"]` への `append` 後にキューサイズが0のまま維持されることを確認済み。

`logic/`層のpublic static func（`DeliveryResolver.matches_order` / `DeliveryResolver.resolve`）はいずれも正常系・異常系・境界値のテストを最低1本ずつ保持しており、`.claude/rules/testing.md`の数え上げ基準を満たす。

## Build

N/A。本プロジェクトはエクスポートプリセットが未確定のため、ビルド検証は対象外。

## Lint

```
$ gdlint atelier/features/guild/ atelier/shared/ atelier/autoload/
Success: no problems found

$ gdformat --check atelier/features/guild/ atelier/shared/ atelier/autoload/
11 files would be left unchanged
```

両方ともPass。

## File Size Check

| ファイル | 行数 | 判定 |
|---|---:|---|
| `atelier/autoload/game_state.gd` | 461 | ⚠️ OK（上限500に対し残り39行） |
| `atelier/features/guild/logic/delivery_result.gd` | 18 | OK |
| `atelier/features/guild/logic/delivery_resolver.gd` | 33 | OK |
| `atelier/features/guild/resources/daily_order_master.gd` | 11 | OK |
| `atelier/shared/constants/game_balance.gd` | 56 | OK |

500行超過ファイルなし。ただし `game_state.gd` は461行で上限に接近しており、後続planでの追記余地が39行しかない（詳細はConfidence Summary 🟡 参照）。

## Confidence Summary

### 🟡 要確認（Medium Confidence）

- **`game_state.gd`が461行で500行ルールの上限に接近**: 残り余地は39行。rank plan（ランクノルマ反映）・日次サイクルplan（`_current_daily_order`再抽選）はいずれも`GameState`への追記を伴うため、次planの着手前に責務分割（例: 納品処理を`GuildDeliveryService`相当へ切り出す、あるいはAutoloadを機能別に分割する）の要否を判断することを推奨する。
- **`get_state()["current_daily_order"]`が`DailyOrderMaster`の参照をそのまま返している**（`atelier/autoload/game_state.gd:71-73`）: 現時点では`DailyOrderMaster`が`@export var`のプリミティブフィールドのみで構成されネストした`Array`/`Dictionary`を持たないため意図的な設計判断（FR-408の対象を`pending_products`に限定）であり、コード上にもその旨のコメントが明記されている。ただし将来`DailyOrderMaster`に配列フィールド（例: 複数の対象特性リスト）を追加した場合、`state-management.md`が求める防御的コピー原則の穴になる。フィールド追加時は`get_state()`側の複製方針を必ず再評価すること。
- **`ProductValueCalculator`の特性ボーナス系統別合成API**（`atelier/features/alchemy/logic/product_value_calculator.gd:3-4`）: 実装ファイル内に「core-systems.mdのクラス図に無い追加分のため設計文書側への反映（同期）を別途検討すること」との🟡マーカーが残存している。これはalchemy plan由来の既存項目でguild planの成果物ではないが、未解消のまま残っている。
- **`DeliveryResolver._multiply_bonus`相当の未登録タグ扱い**: `ProductValueCalculator._multiply_bonus`（同ファイル:35）に🟡マーカーがあり、倍率テーブル未登録タグを黙って無視する挙動が意図通りかの確認が残っている（同じくalchemy plan由来の既存項目）。

### 🔴 要判断（Low Confidence）

- **`GameState._accumulated_contribution`・`_current_daily_order`は暫定フィールドである**（`atelier/autoload/game_state.gd:37-41`）: いずれも本plan内で🔴マーカー付きの新規補完として追加されたものであり、正式実装への置き換えが前提となっている。
  - `_accumulated_contribution`（FR-006, CON-004）: RankSystemが未実装のため、ランクノルマへの反映を行わず累積値のみを保持する暫定措置。**後続のrank planが、この累積値をランクノルマ消費へ接続する正式実装へ置き換える想定**。rank plan着手時にこのフィールドの所有者（`GameState`に残すか`RankSystem`側へ移すか）を必ず判断すること。
  - `_current_daily_order`（CON-010）: 日替わり指定調合物の再抽選ロジックが別planのため、既定値`null`のままでも納品が成立するよう設計されている。**後続の日次サイクルplanが、ターン開始時の抽選処理を実装してこのフィールドの設定経路を正式化する想定**。現状の唯一の設定経路はテスト専用API`_set_current_daily_order_for_test()`であり、本番コードパスからこのフィールドが設定されることは一切ない。
- **`delivered`シグナル・キュー消費順序・`_gold`即時加算・`roundi()`丸めは設計文書に根拠を持たない本plan内の新規決定**（FR-105〜109, CON-007, CON-009）: `core-systems.md`のクラス図には`DeliveryResolver.resolve`の呼び出しとボーナス適用ロジックまでしか記載がなく、納品キューの消費戦略（全件アトミック消費）・報酬の丸めタイミング（納品1件ごとに`roundi()`、合計後ではない）はヒアリング結果に基づく補完である。**特に「1件ごとに丸める」実装（`atelier/autoload/game_state.gd:268`）は、多数の小額報酬を納品した場合に合計後丸めと結果が乖離する**（例: 0.4を10件納品すると1件ごと丸めでは+0、合計後丸めでは+4）。この挙動が意図通りかはバランス設計上の判断であり、`balance-design.md`への明記を推奨する。

### ✅ 検証済み: 指定合致ボーナスの二重適用なし（AC-014, FR-403）

`match_bonus_multiplier` を用いた乗算がコードベース全体で1箇所のみであることをGrepで確認した。

- 乗算箇所: `atelier/features/guild/logic/delivery_resolver.gd:29`
  ```gdscript
  var multiplier := daily_order.match_bonus_multiplier if order_matched else 1.0
  ```
  この`multiplier`が同ファイル31-33行で`product.contribution`・`product.reward`にそれぞれ1回だけ乗算される。
- `ProductValueCalculator`側は**引数として合致ボーナスを受け取る口自体が存在しない**（`calculate_contribution(base_contribution, quality_mult, trait_bonus)` / `calculate_reward(base_reward, quality_mult, trait_bonus)`）。構造的に二重適用が不可能な設計になっており、ソース内コメント（同ファイル:9-10, 18）でも二重乗算バグ（FR-406）の再発防止が明示されている。
- その他のヒット箇所はすべてテストフィクスチャでの値設定、または`GameBalance.DAILY_ORDER_MATCH_BONUS_MULTIPLIER`定数の宣言・値検証であり、乗算には使われていない。
- 統合レベルでも `test_調合から納品までの一連で合致ボーナスが1回だけ適用される`（`atelier/tests/integration/test_game_state_deliver_pending_products.gd:196`）が`execute_alchemy` → `deliver_pending_products`の経路で期待値と突き合わせて検証している。

## Issues Found

なし。

全6タスクが`status: done`、全229テストがPass（失敗・スキップ・orphanともに0件）、gdlint/gdformatともにクリーン、500行超過ファイルなし。Test Strategy項目36件はすべて対応するテストコードに反映されている。

上記Confidence Summaryに挙げた項目はいずれも「現時点の実装バグ」ではなく、後続planへの引き継ぎ事項またはバランス設計上の判断待ち事項である。

## Recommendations

1. **rank plan着手時に`_accumulated_contribution`の正式化を最優先で扱う**: 現在は累積するだけでランクノルマへ接続されていない暫定フィールドであり、rank planのスコープ定義時にこのフィールドの所有権（`GameState`に残すか`RankSystem`へ移譲するか）を明示的に決めること。
2. **`game_state.gd`の責務分割を次plan着手前に検討する**: 461行で上限500まで残り39行。rank planと日次サイクルplanの両方が`GameState`への追記を伴うため、先に分割方針を決めておかないと実装途中で500行ルールに抵触する可能性が高い。
3. **報酬の丸めタイミングを`balance-design.md`へ明記する**: 「納品1件ごとに`roundi()`」という現実装は合計後丸めと結果が乖離しうるため、意図的な仕様であることをバランス設計文書に記録し、将来の実装者が誤って変更しないようにする。
4. 上記が整理できていれば、次はrank planまたはUI planの着手を推奨する。

VERIFY_ALL_PASSED
