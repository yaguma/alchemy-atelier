# アトリエ錬金術ゲーム 要件定義書

作成日: 2026-08-03
準拠コンセプト: [`../../concept/atelier-concept.md`](../../concept/atelier-concept.md)（v7.0・調合主軸モデル）

> 本書は `atelier-concept.md` の内容を実装可能な要件に落とし込んだものである。数値の多くは仮置き（TBD）であり、バランス調整フェーズで確定する。構造的な決定（勝敗条件・昇格試験の性質）はコンセプト文書に明記がなかったため、本書作成時にヒアリングの上で確定した。

## 概要

- ジャンル: 素材収集×調合×資源管理RPG（ギルドランク制）
- 想定プレイ時間: 1ランク攻略 10〜20分 ／ 全ランク攻略 60〜90分（自由に中断可能。ただしセーブ/ロード機能自体は現バージョンの設計スコープ外）
- プラットフォーム: PC（Godot 4.x + GDScript）

---

## 1. プレイサイクル

主戦場は「② 調合」。庭（① 仕込み・戦略層）と納品（③ 決算・自動）が調合を挟んで連結する三段構成。

```
① 状況提示
   ランクHP残量、本日の指定調合物、素材在庫、庭の育成状況、残りターン数が表示される

② プレイヤーの選択
   - 庭: 何を植えるか（指定調合物から逆算）／収穫するか（Bで抜くかS狙いで賭けるか）
   - 調合: 投入枠（4枠）に在庫の素材をどう組み合わせるか
     （品質を盛るか、枠を割いて特性〔貢献度向き／報酬向き〕を宿すか）

③ 結果反映
   - 調合を実行した場合: 完成品が自動でギルドに納品される
     → ランクHPが減少（貢献度） and/or ゴールド獲得（報酬）
     → 指定調合物に合致していればボーナス
     → 投入した素材が在庫から消費される
   - ターンを終了すると（調合を実行したかどうかに関わらず、毎ターン必ず）:
     → 庭の生育がターン経過分だけ進む

④ フィードバック
   貢献度ダメージ演出、報酬獲得表示、指定合致ボーナス表示、ランクHPバーの変化

→ ①に戻る（次のターンへ）
```

このターンループの外側に、以下2層のより大きなループがある。

- **ランクループ**: 制限ターン内にランクHPを0にできれば昇格試験へ。0にできなければ即座に降格（＝ランク文字は下がらず、同一ランクに留まって再挑戦。詳細は「2. 勝敗条件」参照）。
- **ゲーム全体ループ**: G→F→E→D→C→B→A→Sランクを順に攻略し、Sランクの昇格試験クリアでゲームクリア。

---

## 2. 勝敗条件

- **勝利条件**: 最高ランク（S）の昇格試験をクリアする（ゲームクリア）
- **敗北条件**: 同一ランクで規定回数（TBD、仮に3回）連続して降格した場合、ゲームオーバー
  - ヒアリングにより「降格が続いても無限にやり直せるコージーゲーム」ではなく、**明確な詰み状態を持つ**方針で確定
- **「降格」の定義**: 本書における「降格」は**ランク文字（G→F→E→D→C→B→A→S）が下がることではなく、同一ランクに留まって再挑戦すること**を指す。ゲーム全体ループは常に一方向（昇格のみ）で進行し、ランク文字が実際に下がる遷移は存在しない。「降格回数」は同一ランクでの再挑戦回数のカウンタである
- **継続不能条件**: なし。デッキ切れのようなシステムはなく、素材が尽きても庭で仕込み直せば回復できる
- **昇格試験**: 通常ターンループとは別の**一発勝負の特殊局面（ボス戦的な専用画面・専用ルール）**として実装する
  - 成功 → 次ランクへ昇格し、工房強化画面（恒久投資）へ
  - 失敗 → 降格（同一ランクに留まって再挑戦。降格回数カウントが規定回数に達するとゲームオーバー）
  - 具体的なルール・演出は本書のスコープ外。別途設計が必要（🟡 TBD）
- **中断/撤退条件**: セッションはいつでも中断可能。ペナルティなし（ただしセーブ/ロード機能自体が現バージョンでは未実装スコープのため、実際の「再開」体験は別途検討が必要）

---

## 3. プレイヤー操作

### 庭（仕込み層）
- 種を植える（手持ちの種から選ぶ。マス配置なし、スロット数のみの制限）
- 収穫する（今のタイミングで抜くか、待って品質を上げるか判断。待つほど枯死リスクが上がる）
- 種を指名買いする（狙った特性の弾をピンポイントで購入）

### 調合（主戦場）
- 投入枠（4枠、強化で+1）に在庫から素材を選んで入れる
- 投入を入れ替える/取り消す
- 調合を実行する（投入枠に最低1個以上の素材が入っている場合のみ実行可能。0個投入では実行不可）

### ギルド納品
- プレイヤー操作なし（自動決算）。結果の確認のみ

### 工房強化・ショップ
- 恒久投資を購入する（投入枠+1／庭拡張／レシピ解禁）※ランク間の工房強化画面でのみ
- 消耗投資を購入する（触媒／種の指名買い）※ターン中いつでも
- いずれの購入も、所持ゴールドが価格未満の場合は購入不可

### ターン進行
- ターンを終了する（次ターンへ進める）

### 昇格試験
- 特殊局面に挑む（具体的な操作は🟡 TBD、別途設計）

---

## 4. ゲーム要素

### プレイヤー（錬金術師）
- 状態: 所持ゴールド、現在ランク、現ランクでの残りターン数、恒久投資状況（投入枠数・庭スロット数・解禁レシピ一覧）、同ランクでの降格回数
- 変化: ゴールド増減、ランク昇格/降格、投入枠拡張、庭スロット拡張、レシピ解禁

### 素材（Material）
- 状態: 品質スコア（D〜S＝1〜5）、特性タグ（0〜2個）、種別（成熟ターン数が種別ごとに異なる）
- 変化:
  - 庭での生育がターン経過で進行する
  - 収穫時に特性が1つ乱数で付与される（乱数は**分散のみ**で**期待値は動かさない**。種やシナジーで狙いを絞れる。`atelier-concept.md` L205 に準拠）
  - 成熟後、規定ターンを超えて放置すると枯死（全損）する
- 在庫上限: 収穫後・調合前の素材在庫に上限を設けるかは未定（🟡 TBD。「7. 未確定事項」参照）

### 特性タグ（Trait）
- 種別:
  - 貢献度系（聖・浄・癒）
  - 報酬系（金・華・稀）
  - 補助系（触媒＝品質+1段 等）※「触媒」はショップで購入できる、補助系特性タグ付きの素材を指す（「ショップ／工房強化」の消耗投資と同一概念）。購入後は在庫に加わり、他の素材と同様に調合投入枠へ入れて使う
- 発現条件: 調合の投入枠に**同一特性タグの素材を2個以上**投入すると成果物に発現する（1個のみでは不発）。3個目以降を追加投入してもボーナスは据え置き（2個目以降の重複による追加強化はない）

### 調合物（Product）
- 状態: 品質（投入素材の平均品質）、宿った特性（0〜複数）
- 価値計算:
  - `貢献度 = 基礎貢献度 × 品質倍率 × 貢献度系特性ボーナス × 指定合致ボーナス`
  - `報酬 = 基礎報酬 × 品質倍率 × 報酬系特性ボーナス × 指定合致ボーナス`

### 調合台（投入枠）
- 状態: 枠数（初期4、恒久投資で+1）
- 制約: 枠数を超えて同時投入できない。また、**最低1個以上の素材が投入されていなければ調合を実行できない**（0個投入では実行不可）

### ギルドランク（敵）
- 状態: ランクHP、残りターン数（制限ターン）、現在の日替わり指定調合物
- 変化: 調合物の納品でHPが減少する（0未満にはならず0でクランプする。オーバーキル分は切り捨て）。制限ターン到達時にHPが0なら昇格試験、0でなければ降格（同一ランクへの再挑戦。「2. 勝敗条件」参照）

### 日替わり指定調合物（疑似依頼）
- 状態: 本日ギルドが求めている調合物の条件（品目 or 特性傾向）
- 変化: 日ごとに更新される。合致した納品に貢献度・報酬ボーナスが付く

### ショップ／工房強化
- 恒久投資（ランクをまたいで残る）: 投入枠+1（最高額）、庭拡張（高額）、レシピ解禁（高額）
- 消耗投資（その場で消費）: 触媒（補助系特性タグ「触媒」を持つ素材）の常備・購入（中額）、種の指名買い（安価）
- 価格序列: `投入枠+1 ≫ 庭拡張 ≒ レシピ解禁 ＞ 触媒 ＞ 種の指名買い`
- 所持ゴールドが価格未満の場合、いずれの購入も不可（「3. プレイヤー操作」参照）
- 金でターン制限を延長する手段は意図的に存在しない（時間は買えない）

---

## 5. 数値設計（仮置き）

| 変数 | 範囲 | 初期値 | 備考 |
|------|------|--------|------|
| 調合投入枠数 | 4〜5 | 4 | 恒久投資で+1 |
| 品質スコア | 1〜5（D〜S） | - | 旧Phaser版 `calculateQuality` の計算式・アルゴリズム仕様をGDScriptで再実装する（コードの直接移植ではなく仕様の移植。旧実装は本リポジトリには存在しない） |
| 特性発現閾値 | 固定 | 2個 | 同一特性タグを2個以上投入で発現（確定仕様） |
| ランク制限ターン数 | 未定（仮10〜20） | 仮15 | 🟡 TBD。ランクが上がるほど厳しくする想定 |
| 同ランク連続降格の許容回数 | 未定（仮2〜3） | 仮3 | 🟡 TBD。この回数目の降格でゲームオーバー |
| 庭スロット数 | 未定（仮4〜8） | 仮5 | 🟡 TBD。恒久投資「庭拡張」で増加 |
| 素材の成熟ターン数 | 種別ごとに1〜5 | - | 🟡 TBD。早熟＝薬草(1〜2)、晩成＝希少鉱石(4〜5)等を想定 |
| 枯死猶予ターン数 | 未定（仮2〜3） | 仮2 | 🟡 TBD。成熟後の放置上限 |
| ゴールド初期値 | 未定（仮50〜200） | 仮100 | 🟡 TBD |
| 品質倍率 | 未定（仮1.0〜2.0倍） | 仮1.0倍 | 🟡 TBD。旧Phaser版 `calculateAverageQualityMultiplier` の計算式・アルゴリズム仕様をGDScriptで再実装する予定（コードの直接移植ではなく仕様の移植） |
| 基礎貢献度／基礎報酬 | 未定（調合物ごと） | - | 🟡 TBD。レシピマスターデータとして定義 |
| 指定合致ボーナス倍率 | 未定（仮1.2〜1.5倍） | 仮1.2倍 | 🟡 TBD |

---

## 6. 想定プレイフロー

### Gランク・1ターン目（チュートリアル、特性は封印）
- 状況: ランクHP100%、本日の指定調合物「回復薬」、素材は未所持、庭は空
- 行動: 庭に薬草の種を植える → ターンを終了する
- 結果: 庭で薬草の生育が始まる（特性はまだ解禁されていないため品質のみを意識する）

### Gランク・数ターン後
- 状況: 薬草が収穫可能（放置すればS狙いも可能だが、今回はBで確定）
- 行動: 収穫する → 調合台の4枠すべてに薬草（品質B相当）を投入 → 調合を実行する
- 結果: 品質B相当の回復薬が完成 → 自動でギルドに納品 → 指定調合物に合致しボーナス込みでランクHPが減少

### Eランク以降（特性解禁後）
- 状況: 本日の指定調合物は「貢献度重視の品」。在庫には品質A・無特性の素材が2個、特性(聖)付きの品質B素材が2個ある
- 行動: 投入枠4枠すべてを埋める。特性(聖)持ちの品質B素材2個（発現条件を満たす最低数）＋品質A・無特性の素材2個（残り2枠を最高品質で補い、品質低下を抑える判断）を投入する
- 結果: 貢献度系特性(聖)が発現し、品質はA4個で埋めた場合（品質A相当）よりやや落ちる（A×2・B×2の平均）が、貢献度ボーナスの乗った調合物が完成 → 大きくランクHPを削る

### ランクの制限ターン到達時
- 状況: 残りターンが0になり、ランクHPが0まで削り切れている
- 行動: 昇格試験（一発勝負の特殊局面）に自動で移行し挑戦する
- 結果:
  - 成功 → 次ランクへ昇格、工房強化画面で恒久投資を選択できる
  - 失敗 → 降格。同ランクでの降格回数が加算される。規定回数（仮3回）に達していればゲームオーバー、達していなければ同ランクに再挑戦

---

## 7. 未確定事項（🟡 TBD・後続の設計/バランス調整で確定）

- ランク制限ターン数の具体値（ランクごとの傾斜含む）
- 同ランク連続降格の許容回数
- 庭スロット数、素材種別ごとの成熟ターン数、枯死猶予ターン数
- 基礎貢献度／基礎報酬／品質倍率／指定合致ボーナス倍率などレシピ単位の数値
- 昇格試験（一発勝負の特殊局面）の具体的なルール・演出・敗北条件
- ゴールド初期値、ショップ価格の具体額
- 庭の素材種類・特性タグの網羅リスト（貢献度系3種・報酬系3種・補助系1種は確定済み、追加種の有無は未定）
- 収穫後・調合前の素材在庫に上限を設けるか（無制限を意図しているか、上限を設けるか）

---

## 8. 参考文書

- コンセプト設計: [`../../concept/atelier-concept.md`](../../concept/atelier-concept.md)
- 体験コア（v4.0のまま、参照時はatelier-concept.mdを優先）: [`../../concept/experience-core.md`](../../concept/experience-core.md)
